<UserControl
    x:Class="SoundProcessing.Controls.MainMDI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoundProcessing.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ViewModels"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="900">

    <Grid>
        <muxc:TabView x:Name="MdiArea" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AllowDropTabs="False" 
                      IsAddTabButtonVisible="False" TabItemsSource="{Binding Contents, Mode=OneWay}">
            <muxc:TabView.TabItemTemplate>
                <DataTemplate x:DataType="vm:TabContentViewModel">
                    <local:TabContentView />
                </DataTemplate>
            </muxc:TabView.TabItemTemplate>
        </muxc:TabView>
    </Grid>
</UserControl>
